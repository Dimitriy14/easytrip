package bankRatingController

import (
	"net/http"
	"testing"
)

func TestController(t *testing.T) {
	tests := []struct {
		description        string
		url                string
		expectedStatusCode int
		expectedBody       string
	}
}
